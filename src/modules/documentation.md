Imovo :: modules :: documentation
=================================

For consistency and as a convention, a module build script must take exactly one
parameter, the product name:

```
product="$1"
```

That way it's much easier to implement the build of a product.
